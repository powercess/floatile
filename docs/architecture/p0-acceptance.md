# P0 验收标准

> 状态：Accepted
> 证据：尚未完成跨平台验证。
> 验收在目标环境实测，指标可复现。下表平台列是设计预期，不是实测结果；P0 实测另行留档。
> 度量工具：`/proc` 采样、`time`、宿主内置 `--perf` 诊断模式（采样 CPU/RSS/帧率）。

## 1. 功能验收

| # | 项目 | 验收标准 | Win 预期 | macOS 预期 | X11 预期 | Wayland 预期 |
|---|------|----------|----------|------------|----------|---------------|
| F1 | 透明无边框窗口 | 时钟显示无边框无窗口装饰，背景透明 | 支持 | 支持 | 条件支持：需合成器 | 支持 |
| F2 | Always-on-top | 置顶后盖过普通窗口 | 支持 | 支持 | 支持 | 条件支持/降级 |
| F3 | 点击穿透 | 开启穿透后鼠标可点到底层应用；编辑模式下关闭穿透，可正常交互 | 支持 | 支持 | 支持 | 不支持，必须降级 |
| F4 | 编辑/展示模式切换 | 编辑模式显示边框/手柄/删除/设置；展示模式隐藏全部宿主控件 | 支持 | 支持 | 支持 | 支持 |
| F5 | 拖拽 | 编辑模式自由拖拽，位移实时跟随 | 支持 | 支持 | 支持 | 支持 |
| F6 | 缩放 | 支持尺寸范围拖拽缩放，min/max 生效 | 支持 | 支持 | 支持 | 支持 |
| F7 | 多显示器 | 可拖到第二屏，位置坐标正确 | 支持 | 支持 | 支持 | 支持 |
| F8 | 显示器热插拔恢复 | 拔屏再插回，布局恢复到原位置 | 支持 | 支持 | 条件支持 | 支持 |
| F9 | 布局持久化 | 重启后布局/尺寸/模式恢复 | 支持 | 支持 | 支持 | 支持 |
| F10 | 硬编码时钟 | 每秒更新时间，逻辑正确 | 支持 | 支持 | 支持 | 支持 |
| F11 | 插件化时钟 | `.slint + .wasm` 加载、计时器回调、UI 属性更新 | 支持 | 支持 | 支持 | 支持 |
| F12 | 恶意插件被拒 | 未授权能力调用返回 permission denied，宿主不崩溃，审计留痕 | 支持 | 支持 | 支持 | 支持 |
| F13 | 能力矩阵回填 | platform-matrix 三端实测数据完整 | 必测 | 必测 | 必测 | 必测 |

## 2. 性能验收（目标值，P0 实测后更新）

| 指标 | 目标 | 实测 |
|------|------|------|
| 空宿主空闲 CPU（无 widget，展示模式） | ≤1.0% 单核平均 | |
| 空宿主内存 RSS（release） | ≤80 MiB | |
| 单时钟实例空闲 CPU（60 FPS） | ≤3.0% 单核平均 | |
| 10 时钟实例平均帧率 | ≥55 FPS（集显） | |
| 冷启动到首帧（release） | ≤1.5 s | |
| 布局持久化加载 | ≤200 ms（10 实例） | |
| 恶意插件拒绝调用时宿主无卡顿 | 拒绝路径 ≤1 ms 平均 | |

## 3. 安全验收（P0 必测）

构造一个恶意测试插件（`tests/fixtures/evil-plugin`），行为：

1. 调用未声明的能力（如读 `storage:read` 之外、尝试访问宿主内存地址 `0x...`）。
2. 尝试绕过接口直接调用宿主内部函数（应无法获得任何句柄/符号）。
3. 无限循环占用 CPU（验证 fuel 上限触发 trap）。
4. 超大内存申请（验证线性内存上限）。
5. 尝试构造文件名 `../../` 读取宿主文件（P0 无文件接口 → 应无路径；验证无崩溃）。

通过标准：

- 1/3/4/5 → 拒绝/终止 + 审计记录 + 宿主进程存活。
- 2 → 编译或链接失败（无宿主符号暴露）；若运行期可调用则直接判失败。
- 宿主被整死、无审计、或插件读取到宿主内任意内存 → 判失败。

## 4. 工程验收

- `cargo clippy -D warnings`、`cargo test`（core/store/platform 单测）在三端 CI 全绿。
- WIT 单一源校验：`floatile-plugin-api` 与 `floatile-sdk` 绑定同源生成（CI 检查）。
- 无 `unsafe` 泄漏到业务 crate（仅 `floatile-platform` 允许隔离的 unsafe 且带注释与测试）。
- 三端各自可以 `cargo build --release` 且产物可运行。
- Wayland 降级路径在至少一个纯 Wayland 环境（如 sway）与一个 GNOME/Wayland 环境实测。

## 5. 失败判定

- 任一平台核心项（F1/F3/F11）无法实现且无明确降级说明 → P0 视为「暴露风险成功」，需写复盘报告决定 MVP 范围。
- P0 核心目的是暴露风险而非堆砌功能；「失败」= 隐瞒或绕开问题，而不是问题本身。

## 6. 交付记录模板

每个验收项：`测试环境 | 操作步骤 | 结果 | 截图/日志 | 备注`。汇总到 `docs/platform-matrix/platform-matrix.md` 的回填表。
